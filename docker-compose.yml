version: "3.9"

services:
  mongodb:
    image: mongo:7.0
    container_name: mercora-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7.2
    container_name: mercora-redis
    ports:
      - "6379:6379"

volumes:
  mongo_data:
